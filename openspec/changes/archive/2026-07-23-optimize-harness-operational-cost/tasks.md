## 1. Apply Readiness and Migration Baseline

- [x] 1.1 [orchestrator] Run native apply preflights, recompute the approved planning digest, and create current schema-v2 progress/approval evidence before the first implementation edit.
- [x] 1.2 [orchestrator] Record the `standard-change` ownership plan, editable roots, exact skills, compact-inline intent, schema-v3 migration boundary, and requirementless documentation impact.

## 2. Deterministic Planning and Verification Primitives

- [x] 2.1 [orchestrator] Add dependency-free planning path collection and SHA-256 digest support to the harness CLI, including deterministic ordering, linked-requirement coverage, unsafe-path rejection, and focused tests.
- [x] 2.2 [orchestrator] Add the dependency-free final verification runner with the five ordered gates, single execution, streaming output, bounded summaries, per-gate and aggregate timings, fail-fast behavior, ignored JSON result output, and injected-process tests.
- [x] 2.3 [orchestrator] Change `pnpm verify` to invoke only the timed runner while preserving `verify:fast`, non-incremental final typecheck, full application/reference lint, production build, and zero-exit semantics.

## 3. Schema-v3 Evidence and Mechanical Validation

- [x] 3.1 [orchestrator] Replace schema-v2 delegation/prose handoff validation with schema-v3 assurance profile, ownership plan, compact `executionRecords`, conditional subagent/fallback fields, task coverage, one-writer ownership, and approvalCheckpoint-v2 digest validation.
- [x] 3.2 [orchestrator] Migrate this active change atomically to schema-v3 progress when the validator contract changes, preserving the approved checkpoint, completed/pending task reconciliation, files, decisions, and problems.
- [x] 3.3 [orchestrator] Add conservative accepted/delta `MODIFIED` requirement and scenario identity compatibility validation before expensive final gates.
- [x] 3.4 [orchestrator] Extend positive and negative Node fixtures for assurance profiles, compact inline records, subagent/fallback evidence, planning-digest staleness, duplicate/missing final gates, incompatible deltas, scoped no-change evidence, and archived-schema immutability.

## 4. Compact Governance and Lazy Skill Context

- [x] 4.1 [orchestrator] Consolidate `AGENTS.md`, SDD, registry, and orchestrator guidance so lifecycle policy, skill mapping, and role decisions have non-duplicated authoritative ownership and the root bootstrap stays within its word budget.
- [x] 4.2 [orchestrator] Update the phase-handoff contract, implementation-progress/verification skills, specialized roles, and runtime adapters for assurance profiles, digest-bound approval, schema-v3 structured execution records, compact inline evidence, conditional documentation reconciliation, and risk-proportional verification.
- [x] 4.3 [orchestrator] Compact data/UI domain skills by retaining rules, decisions, naming, and checklists while replacing long embedded examples with exact lazy `.agent/reference/widget/` paths; enforce root, UI, and data context budgets mechanically.
- [x] 4.4 [orchestrator] Update local OpenSpec propose/apply/update/explore/sync/archive overlays to preserve assurance routing, planning digests, non-duplicated packet reuse, schema-v3 progress, timed single-run evidence, compatibility preflight, and fail-closed archive semantics.

## 5. Documentation and Focused Reconciliation

- [x] 5.1 [orchestrator] Reconcile `.agent/README.md`, `README.md`, and `harness-docs/` with the three assurance profiles, scoped no-change matrix, compact evidence model, digest-bound approval, conditional documentation reconciliation, single-run final command, and authoritative-source links.
- [x] 5.2 [orchestrator] Record documentation reconciliation as `not-applicable` for a requirement brief and `updated` for the affected harness guides before final verification.

## 6. Provisional Verification and Contract Reconciliation

- [x] 6.1 [orchestrator] Run syntax checks, runner-focused tests, complete harness validator fixtures, strict OpenSpec validation, `pnpm validate:harness`, word-budget checks, and one warm `pnpm verify:fast`; reconcile every failure without running the final aggregate command.
- [x] 6.2 [orchestrator] Reread proposal, delta specs, design, tasks, schema-v3 progress, command definitions, and affected guidance; confirm implemented scope and accepted scenario identities are coherent before final verification.

## 7. Final Verification

- [x] 7.1 [agent-verifier] Perform final conformance review and execute exactly one authoritative `pnpm verify` through the timed runner; return structured gate evidence without replaying successful commands or repairing implementation.
