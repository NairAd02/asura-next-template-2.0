# Tasks

## 1. OpenSpec Setup

- [x] 1.1 Install OpenSpec CLI.
- [x] 1.2 Initialize OpenSpec for Codex.
- [x] 1.3 Configure `openspec/config.yaml` with `.agent` context and artifact rules.

## 2. Requirements Layer

- [x] 2.1 Create `docs/project-context.md`.
- [x] 2.2 Create `docs/requirements/` with index and brief template.
- [x] 2.3 Add `REQ-001` as the pilot requirement brief.

## 3. `.agent` Integration

- [x] 3.1 Add `requirements-curation` skill.
- [x] 3.2 Add `agent-requirements-curator`.
- [x] 3.3 Replace the old spec-writer path with requirements curation and OpenSpec.
- [x] 3.4 Update registry, orchestrator, architect, data, verifier, and SDD instructions.

## 4. Verification

- [x] 4.1 Run `openspec doctor`.
- [x] 4.2 Run `openspec list --json`.
- [x] 4.3 Run `openspec validate --all --json`.
- [x] 4.4 Run TypeScript, lint, and build gates and document preexisting failures.
