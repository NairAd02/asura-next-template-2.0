## 1. Progress and Lightweight Test Infrastructure

- [x] 1.1 Create the reconciled apply-progress snapshot with requirement/architecture handoffs and exact skills.
- [x] 1.2 Add approved Vitest, Testing Library, jsdom, and Vite adapter development dependencies without a browser framework or binary.
- [x] 1.3 Add Vitest setup/config, server-only shim, generated-output ignores, and test-aware TypeScript boundaries.
- [x] 1.4 Add `test:unit`, cached `typecheck:fast`/`lint:fast`, `verify:fast`, and expanded final `verify` scripts.

## 2. Focused Supplier Coverage

- [x] 2.1 Add deterministic supplier-store reset support without an HTTP testing endpoint.
- [x] 2.2 Add unit tests for schemas/server validation, filter sanitation/conversion, explicit false, search, sort, limits, page 2, and out-of-range clamping.
- [x] 2.3 Add unit tests for normalized CRUD, duplicate/self-exclusion, `NOT_FOUND`, audit behavior, store persistence, and 12-seed reset.
- [x] 2.4 Add a `DataTable` component regression test for unstable initial visibility identity and localStorage persistence.

## 3. Harness Governance

- [x] 3.1 Create and validate the behavior-testing skill, then register exact ownership/phase mappings.
- [x] 3.2 Update SDD, progress, verification, verifier/executor roles, handoff guidance, and human documentation for focused test ownership and short browser smoke.
- [x] 3.3 Update harness validator tests/invariants and accepted-contract references for five command categories plus optional browser evidence.

## 4. Verification and Reconciliation

- [x] 4.1 Run `pnpm verify:fast` twice and record cold/warm durations against the approximately 90-second target.
- [x] 4.2 Run and time `pnpm verify`; require specs/harness, unit/component, non-incremental typecheck, full lint, and build PASS below 15 minutes.
- [x] 4.3 Run the short integrated-browser EN desktop, ES validation, and mobile card/action smoke.
- [x] 4.4 Reconcile every task, progress, files, handoffs, durations, warnings, and REQ-005 close data before final PASS evidence.
