## 1. Prisma guidance boundary

- [x] 1.1 [orchestrator] Refocus the Prisma capability profile, registry, skill, and service-pattern guidance on project-agnostic service architecture and the required official-documentation handoff for setup.
- [x] 1.2 [orchestrator] Remove local Prisma configuration/setup artifacts and update the widget Prisma service reference to use documented project-owned persistence boundaries without prescribing generated-client output paths.

## 2. Dependency-free architecture validation

- [x] 2.1 [orchestrator] Replace configuration-shape validation with a small static contract for Prisma service architecture and the absence of a local setup mirror.
- [x] 2.2 [orchestrator] Update focused positive and negative harness fixtures/tests without installing Prisma or accessing a database, credentials, Docker, or official documentation.

## 3. Verification and evidence

- [x] 3.1 [orchestrator] Validate the OpenSpec delta and run focused harness/reference checks, confirming the implementation has no project-specific Prisma configuration or external-service verification.
- [x] 3.2 [orchestrator] Freeze the approved scope and run the single authoritative `pnpm verify` runner exactly once; reconcile schema-v3 progress and final evidence.
